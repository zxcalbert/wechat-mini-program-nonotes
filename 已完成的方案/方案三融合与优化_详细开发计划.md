# 方案三融合与优化 - 详细开发计划

**项目名称**：个人笔记 - 微信小程序  
**计划版本**：v2.0  
**创建日期**：2026-02-27  
**关联文档**：
- 方案三融合与优化_最终设计报告.md
- .trae/rules/project_rules.md

---

## 一、版本回滚机制

### 1.1 稳定版本标签

| 标签 | 说明 | 回退命令 |
|-----|------|---------|
| `v0.1.0-stable` | 当前稳定版本（方案三开发前） | `git checkout v0.1.0-stable` |

**回滚流程**：
```bash
# 如果需要回退到稳定版本
git checkout v0.1.0-stable

# 或者从当前分支回退
git reset --hard v0.1.0-stable
```

---

## 二、项目开发计划总览

### 2.1 时间规划

| 阶段 | 时间 | 目标 | 交付物 |
|-----|------|------|-------|
| **Phase 1** | Day 1 | 规则库基础 + 前端说明 | M1 |
| **Phase 2** | Day 2 | 完整规则库 + 提示词 + 自适应 | M2 |
| **Phase 3** | Day 3 | API集成 + 测试 + 验收 | M3 + M4 |

### 2.2 项目规范遵循

严格遵循 [`.trae/rules/project_rules.md`](file:///Users/bill/编程/invest-diary/.trae/rules/project_rules.md)：

| 规范项 | 要求 |
|-------|------|
| Git工作流 | feature分支开发，main分支稳定 |
| 提交信息 | `type(scope): subject` 格式 |
| 代码风格 | 2空格缩进，必须使用分号 |
| 异常处理 | 全局异常捕获 + 页面级异常处理 |
| 渐进式迁移 | 禁止一次性重构 |

---

## 三、Phase 1 详细计划（Day 1）

### 3.1 时间安排

| 时间 | 任务 | 负责人 | 交付物 |
|-----|-----|-------|-------|
| **09:00-10:00** | 规则库JSON结构设计 | 后端 | 规则库结构文档 |
| **10:00-12:00** | 查理·芒格/巴菲特规则定义 | 后端 | `mentorRules.json`（2位大师） |
| **14:00-16:00** | 前端动态说明展示 | 前端 | AI助手/心境说明页面 |
| **16:00-18:00** | Phase 1验收 + 测试 | 全团队 | M1交付 |

### 3.2 任务分解

#### 任务1.1：规则库JSON结构设计

**文件**：`cloudfunctions/replyToLetter/mentorRules.json`

**结构设计**：
```json
{
  "mentors": {
    "查理·芒格": {
      "persona": "我是查理·芒格...",
      "corePrinciples": [
        "1. 反过来想，总是反过来想",
        "2. 多学科思维模型",
        "...（共8条）"
      ],
      "thinkingFrameworks": [
        "1. 逆向思考法",
        "...（共5个）"
      ],
      "commonQuestions": [
        "1. 如何避免误判？",
        "...（共6个）"
      ],
      "tone": "睿智、深刻",
      "style": "喜欢用比喻和格言"
    }
  },
  "moods": {
    "焦虑": {
      "tone": "温和安抚",
      "focus": "风险管控、长期价值",
      "keyPoints": [
        "1. 市场波动是常态",
        "...（共5条）"
      ]
    }
  }
}
```

**验收标准**：
- ✅ JSON结构清晰，易于扩展
- ✅ 包含6位大师和4种心境
- ✅ 文档完整，注释清晰

---

#### 任务1.2：查理·芒格/巴菲特规则定义

**查理·芒格核心原则（8条）**：
1. 反过来想，总是反过来想
2. 多学科思维模型
3. 在手里拿着锤子的人看来，世界就像一颗钉子
4. 避免人类误判心理学中的常见陷阱
5. 投资最重要的是避免错误，而不是追求完美
6. 耐心是投资的美德
7. 安全边际是生命线
8. 长期主义，时间是朋友

**查理·芒格思考框架（5个）**：
1. 逆向思考法
2. 多学科思维模型
3. 决策树分析
4. 概率思维
5. 检查清单

**查理·芒格常用问题（6个）**：
1. 如何避免误判？
2. 如何建立自己的思维模型？
3. 如何面对市场波动？
4. 如何选择投资标的？
5. 如何保持耐心？
6. 如何从错误中学习？

**验收标准**：
- ✅ 查理·芒格规则完整（8原则+5框架+6问题）
- ✅ 巴菲特规则完整（8原则+5框架+6问题）
- ✅ 内容经过验证，符合大师风格

---

#### 任务1.3：前端动态说明展示

**修改文件**：
- `miniprogram/pages/write/write.wxml`
- `miniprogram/pages/write/write.js`
- `miniprogram/pages/write/write.wxss`

**功能需求**：
1. 选择AI助手时，点击显示规则说明
2. 选择心境时，点击显示心境说明
3. 说明从规则库动态读取（模拟：先硬编码，Phase 2改为动态）

**实现（Phase 1先硬编码模拟）**：
```javascript
// write.js
Page({
  data: {
    showMentorGuide: false,
    showMoodGuide: false,
    currentMentorGuide: '',
    currentMoodGuide: ''
  },

  showMentorGuideHandler() {
    const guides = {
      '查理·芒格': '核心原则：1. 反过来想...',
      '巴菲特': '核心原则：1. 时间是优秀企业的朋友...'
    };
    this.setData({
      showMentorGuide: true,
      currentMentorGuide: guides[this.data.mentors[this.data.mentorIndex]]
    });
  },

  showMoodGuideHandler() {
    const guides = {
      '焦虑': '当前心境：焦虑。建议：保持理性，关注长期价值...',
      '贪婪': '当前心境：贪婪。建议：注意安全边际，避免冲动...'
    };
    this.setData({
      showMoodGuide: true,
      currentMoodGuide: guides[this.data.selectedMood]
    });
  }
});
```

**验收标准**：
- ✅ 选择AI助手时，可点击查看说明
- ✅ 选择心境时，可点击查看说明
- ✅ 说明展示美观，用户体验良好

---

### 3.3 Phase 1 里程碑验收（M1）

| 验收项 | 验收标准 |
|-------|---------|
| 规则库结构 | JSON结构设计完成，文档齐全 |
| 2位大师规则 | 查理·芒格、巴菲特规则完整 |
| 前端说明展示 | AI助手/心境说明可点击查看 |
| Git提交 | 符合规范，提交信息格式正确 |
| 测试通过 | 基本功能测试通过，无严重bug |

---

## 四、Phase 2 详细计划（Day 2）

### 4.1 时间安排

| 时间 | 任务 | 负责人 | 交付物 |
|-----|-----|-------|-------|
| **09:00-11:00** | 剩余4位大师规则定义 | 后端 | `mentorRules.json`（完整6位） |
| **11:00-12:00** | 4种心境规则定义 | 后端 | `mentorRules.json`（完整4种心境） |
| **14:00-15:00** | 前端动态读取规则库（新增） | 全团队 | 云函数+前端缓存 |
| **15:00-16:00** | 超详细提示词组装器 | 后端 | `getMentorPrompt()` 函数 |
| **16:00-17:00** | 字数自适应引擎 | 后端 | 字数配置 + 截断函数 |
| **17:00-18:00** | Phase 2验收 + 测试 | 全团队 | M2交付 |

### 4.1.1 Phase 1建议项整合

根据Phase 1测试报告的建议，新增以下任务：

| 建议项 | 优先级 | 实施时间 | 状态 |
|-------|--------|---------|------|
| 前端动态读取规则库 | 🔴 高 | Phase 2 14:00-15:00 | ✅ 已采纳 |
| 添加缓存机制 | 🟢 低 | 随前端动态读取一并实施 | ✅ 已采纳 |
| 规则库内容优化 | 🟡 中 | 持续迭代 | ✅ 已采纳 |

### 4.2 任务分解

#### 任务2.1：剩余4位大师规则定义

**剩余4位大师**：
1. 段永平
2. 张小龙
3. 乔布斯
4. 马斯克

**段永平核心原则（8条）**：
1. 做对的事情，把事情做对
2. 本分是最重要的企业文化
3. 企业文化是最重要的护城河
4. 不要做不对的事情，即使能赚钱
5. 慢就是快
6. 买股票就是买公司
7. 长期持有，忽略短期波动
8. 回归本分，保持平常心

**验收标准**：
- ✅ 4位大师规则完整（8原则+5框架+6问题）
- ✅ 所有6位大师规则完整

---

#### 任务2.2：4种心境规则定义

**心境列表**：
1. 焦虑
2. 贪婪
3. 平和
4. 困惑

**焦虑心境规则**：
```json
{
  "tone": "温和安抚",
  "focus": "风险管控、长期价值",
  "keyPoints": [
    "1. 市场波动是常态，保持理性",
    "2. 关注企业内在价值，而非短期价格",
    "3. 安全边际是投资的生命线",
    "4. 不要被恐慌情绪影响决策",
    "5. 时间会平滑短期波动"
  ]
}
```

**验收标准**：
- ✅ 4种心境规则完整（语气+重点+5关键点）
- ✅ 内容经过验证，符合心境特点

---

#### 任务2.3：超详细提示词组装器

**修改文件**：`cloudfunctions/replyToLetter/index.js`

**函数**：`getMentorPrompt(mentor, mood, content)`

**超详细提示词结构**：
```
【规则约束部分（不可变，来自规则库）】
你必须以{mentor}的身份回复，遵循以下核心原则：
1. {corePrinciple1}
2. {corePrinciple2}
...（共8条）

用户当前心境：{mood}
- 语气必须：{moodTone}
- 重点必须：{moodFocus}
- 必须涵盖这5个关键点：{moodKeyPoints}

【超详细自由发挥部分（方案二优势）】
你的完整人设（500字）：
{mentorPersona}

你的思考框架（5个）：
{thinkingFrameworks}

你的常用问题（6个）：
{commonQuestions}

基于以上约束，针对用户的问题：
{content}

【字数自适应要求】
- 先评估内容复杂度：简单/中等/复杂
- 简单问题：200字
- 中等问题：300字
- 复杂问题：400-500字
- 上限：严格500字，不得超过

给出直接、具体、有针对性的回复，200-500字。
```

**验收标准**：
- ✅ 提示词完整，1000+字
- ✅ 规则约束部分清晰
- ✅ 超详细自由发挥部分充分
- ✅ 字数自适应要求明确

---

#### 任务2.4：字数自适应引擎

**修改文件**：`cloudfunctions/replyToLetter/index.js`

**字数配置**：
```javascript
const wordCountConfig = {
  simple: { min: 200, max: 200, maxTokens: 280, label: '简单' },
  medium: { min: 200, max: 300, maxTokens: 400, label: '中等' },
  complex: { min: 400, max: 500, maxTokens: 650, label: '复杂' }
};
```

**复杂度评估**：
```javascript
function estimateComplexity(userContent) {
  const length = userContent.length;
  if (length < 100) return 'simple';
  if (length > 300) return 'complex';
  return 'medium';
}
```

**中文字数统计**：
```javascript
function countChineseWords(text) {
  const chineseChars = text.match(/[\u4e00-\u9fa5]/g) || [];
  const englishWords = text.match(/[a-zA-Z]+/g) || [];
  return chineseChars.length + Math.round(englishWords.length * 1.5);
}
```

**中文字数截断（保留完整句子）**：
```javascript
function truncateByChineseWords(text, maxWords) {
  const currentCount = countChineseWords(text);
  if (currentCount <= maxWords) return text;
  
  // 二分查找截断点
  let low = 0, high = text.length;
  let best = text.substring(0, Math.floor(text.length * 0.7));
  
  while (low <= high) {
    const mid = Math.floor((low + high) / 2);
    const candidate = text.substring(0, mid);
    const count = countChineseWords(candidate);
    
    if (count <= maxWords) {
      best = candidate;
      low = mid + 1;
    } else {
      high = mid - 1;
    }
  }
  
  // 找到最后一个完整句子
  const lastPunc = Math.max(
    best.lastIndexOf('。'),
    best.lastIndexOf('！'),
    best.lastIndexOf('？')
  );
  
  return lastPunc > 0 ? best.substring(0, lastPunc + 1) : best;
}
```

**验收标准**：
- ✅ 字数配置完整
- ✅ 复杂度评估函数正确
- ✅ 中文字数统计准确
- ✅ 截断函数保留完整句子

---

#### 任务2.5：前端动态读取规则库（新增）

**修改文件**：
- `cloudfunctions/getMentorRules/index.js`（新增）
- `miniprogram/pages/write/write.js`
- `miniprogram/utils/cacheUtil.js`（新增）

**实施背景**：
根据Phase 1测试报告建议，将前端说明从硬编码改为从`mentorRules.json`动态读取，实现规则库热更新。

**技术方案**：

1. **云函数实现 (`cloudfunctions/getMentorRules/index.js`)**：
```javascript
const cloud = require('wx-server-sdk');
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV });

exports.main = async (event, context) => {
  const mentorRules = require('../replyToLetter/mentorRules.json');
  return {
    success: true,
    data: mentorRules
  };
};
```

2. **缓存工具 (`miniprogram/utils/cacheUtil.js`)**：
```javascript
const CACHE_KEY = 'mentorRules';
const CACHE_EXPIRE = 7 * 24 * 60 * 60 * 1000;

function saveMentorRulesCache(data) {
  wx.setStorageSync(CACHE_KEY, {
    data,
    timestamp: Date.now()
  });
}

function getMentorRulesCache() {
  const cached = wx.getStorageSync(CACHE_KEY);
  if (!cached) return null;
  if (Date.now() - cached.timestamp > CACHE_EXPIRE) {
    return null;
  }
  return cached.data;
}

module.exports = {
  saveMentorRulesCache,
  getMentorRulesCache
};
```

3. **前端集成 (`miniprogram/pages/write/write.js`)**：
```javascript
const { saveMentorRulesCache, getMentorRulesCache } = require('../../utils/cacheUtil.js');

Page({
  data: {
    mentorRules: null,
    showMentorGuide: false,
    showMoodGuide: false,
    currentMentorGuide: '',
    currentMoodGuide: ''
  },

  onLoad() {
    this.loadMentorRules();
  },

  loadMentorRules() {
    const cached = getMentorRulesCache();
    if (cached) {
      this.setData({ mentorRules: cached });
      return;
    }

    wx.cloud.callFunction({
      name: 'getMentorRules',
      success: (res) => {
        if (res.result.success) {
          const rules = res.result.data;
          this.setData({ mentorRules: rules });
          saveMentorRulesCache(rules);
        }
      },
      fail: (err) => {
        console.error('加载规则库失败:', err);
        this.fallbackToHardcoded();
      }
    });
  },

  fallbackToHardcoded() {
    const mentorGuides = {
      '查理·芒格': '核心原则：1. 反过来想...',
      '巴菲特': '核心原则：1. 时间是优秀企业的朋友...'
    };
    const moodGuides = {
      '焦虑': '当前心境：焦虑。建议：保持理性...'
    };
    this.data.fallbackMentorGuides = mentorGuides;
    this.data.fallbackMoodGuides = moodGuides;
  },

  formatMentorGuide(mentorName) {
    const rules = this.data.mentorRules;
    if (!rules || !rules.mentors[mentorName]) {
      return this.data.fallbackMentorGuides[mentorName] || '';
    }

    const mentor = rules.mentors[mentorName];
    let guide = `核心原则：\n`;
    mentor.corePrinciples.forEach((principle) => {
      guide += `${principle}\n`;
    });
    return guide;
  },

  formatMoodGuide(moodName) {
    const rules = this.data.mentorRules;
    if (!rules || !rules.moods[moodName]) {
      return this.data.fallbackMoodGuides[moodName] || '';
    }

    const mood = rules.moods[moodName];
    let guide = `当前心境：${moodName}\n\n建议：${mood.tone}\n\n关键点：\n`;
    mood.keyPoints.forEach((point) => {
      guide += `${point}\n`;
    });
    return guide;
  },

  showMentorGuideHandler() {
    const selectedMentor = this.data.mentors[this.data.mentorIndex];
    const guide = this.formatMentorGuide(selectedMentor);
    this.setData({
      showMentorGuide: true,
      currentMentorGuide: guide
    });
  },

  showMoodGuideHandler() {
    const guide = this.formatMoodGuide(this.data.selectedMood);
    this.setData({
      showMoodGuide: true,
      currentMoodGuide: guide
    });
  }
});
```

**验收标准**：
- ✅ 云函数`getMentorRules`正常返回规则库JSON
- ✅ 前端成功调用云函数并显示说明
- ✅ 本地缓存机制正常工作
- ✅ 网络异常时有降级到硬编码的备用方案
- ✅ 缓存过期时间设置为7天

---

### 4.3 Phase 2 里程碑验收（M2）

| 验收项 | 验收标准 |
|-------|---------|
| 完整规则库 | 6位大师 + 4种心境规则完整 |
| 超详细提示词 | 提示词组装器完成，1000+字 |
| 字数自适应 | 引擎完整，统计+截断正确 |
| 前端动态读取 | 云函数+缓存机制完成，降级方案完整 |
| Git提交 | 符合规范，提交信息格式正确 |
| 单元测试 | 字数统计、截断函数测试通过 |

---

## 五、Phase 3 详细计划（Day 3）

### 5.1 时间安排

| 时间 | 任务 | 负责人 | 交付物 |
|-----|-----|-------|-------|
| **09:00-11:00** | DeepSeek API集成优化 | 后端 | API集成完成 |
| **11:00-12:00** | 后处理截断 | 后端 | 截断集成完成 |
| **14:00-16:00** | A/B测试 + 优化 | 全团队 | 测试报告 |
| **16:00-18:00** | 完整验收 + 文档 | 全团队 | M3 + M4交付 |

### 5.2 任务分解

#### 任务3.1：DeepSeek API集成优化

**修改文件**：`cloudfunctions/replyToLetter/index.js`

**动态max_tokens**：
```javascript
const config = getWordCountConfig(userContent);
const payload = {
  model: 'deepseek-chat',
  messages: [...],
  temperature: 0.7,
  max_tokens: config.maxTokens  // 动态设置
};
```

**验收标准**：
- ✅ max_tokens根据复杂度动态设置
- ✅ API调用正常，无报错
- ✅ 响应时间在可接受范围内

---

#### 任务3.2：后处理截断

**修改文件**：`cloudfunctions/replyToLetter/index.js`

**集成截断**：
```javascript
// API返回后应用截断
if (response.data && response.data.choices && response.data.choices.length > 0) {
  const choice = response.data.choices[0];
  let reply = choice.message?.content;
  
  if (reply) {
    const config = getWordCountConfig(userContent);
    // 强制截断到上限
    reply = truncateByChineseWords(reply.trim(), config.max);
    console.log('DeepSeek API 调用成功，生成了', reply.length, '字符');
    return reply;
  }
}
```

**验收标准**：
- ✅ 截断在API返回后正确应用
- ✅ 超过上限的回复被正确截断
- ✅ 截断保留完整句子

---

#### 任务3.3：A/B测试 + 优化

**测试维度**：
1. **回复质量**：200-500字，内容完整
2. **一致性**：规则约束正确应用
3. **个性化**：提示词自由发挥充分
4. **性能**：响应时间，资源占用
5. **用户体验**：前端说明展示流畅

**验收标准**：
- ✅ A/B测试完成，有对比数据
- ✅ 优化建议明确，可执行
- ✅ 主要问题已修复

---

#### 任务3.4：完整验收 + 文档

**验收清单**：
- ✅ 所有功能完成
- ✅ 所有测试通过
- ✅ 文档完整
- ✅ Git提交规范

**交付物**：
- 完整功能代码
- 测试报告
- 用户手册
- 技术文档

---

### 5.3 Phase 3 里程碑验收

| 里程碑 | 验收项 |
|-------|---------|
| **M3** | API集成 + 后处理完成 |
| **M4** | 完整功能 + 测试 + 文档完成 |

---

## 六、风险评估与应对

| 风险 | 等级 | 应对策略 |
|-----|------|---------|
| 规则定义质量不高 | 🟡 中 | 多位成员review，迭代优化 |
| 提示词效果不佳 | 🟡 中 | A/B测试，收集反馈快速调整 |
| 开发周期超出预期 | 🟡 中 | 增量开发，MVP先行 |
| API响应变慢 | 🟢 低 | 提示词优化，缓存机制 |
| 截断影响语义 | 🟡 中 | 保留完整句子，人工review |

---

## 七、资源分配

| 资源 | 需求 | 说明 |
|-----|------|------|
| **人力资源** | 后端1人 + 前端1人 | 可兼职，总计3人天 |
| **时间** | 3天 | 按8小时/天计算 |
| **软件资源** | 微信开发者工具、DeepSeek API | 已有 |
| **硬件资源** | 测试设备（iPhone + Android） | 建议 |

---

## 八、交付物清单

| 交付物 | 文件路径 | 状态 |
|-------|---------|------|
| 规则库JSON | `cloudfunctions/replyToLetter/mentorRules.json` | Phase 2 |
| 超详细提示词 | `cloudfunctions/replyToLetter/index.js` | Phase 2 |
| 字数自适应引擎 | `cloudfunctions/replyToLetter/index.js` | Phase 2 |
| 前端动态说明 | `miniprogram/pages/write/` | Phase 1 |
| API集成优化 | `cloudfunctions/replyToLetter/index.js` | Phase 3 |
| 测试报告 | `docs/测试报告.md` | Phase 3 |
| 完整功能代码 | 全项目 | Phase 3 |

---

## 九、Git提交规范

### 9.1 分支管理

```
main (稳定版本)
 └── feature/scheme3-hybrid (开发分支)
       ├── feat/rule-library (规则库)
       ├── feat/prompt-builder (提示词)
       └── feat/word-count-adaptive (字数自适应)
```

### 9.2 提交信息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

**示例**：
```
feat(rule): 添加查理·芒格和巴菲特规则

- 8条核心原则
- 5个思考框架
- 6个常用问题

Closes #1
```

---

## 十、总结与下一步行动

### 10.1 总结

- ✅ 版本回滚机制已建立（v0.1.0-stable）
- ✅ 详细开发计划已制定（3天，3个Phase）
- ✅ 任务分解清晰，可执行
- ✅ 风险评估与应对策略完整
- ✅ 遵循项目规范，渐进式开发

### 10.2 下一步行动

1. **立即执行**：确认计划，启动Phase 1
2. **Phase 1目标**：Day 1完成规则库基础 + 前端说明
3. **保持沟通**：每日同步进度，及时调整
4. **保留回退**：如遇问题，可随时回退到v0.1.0-stable

---

**文档结束**
