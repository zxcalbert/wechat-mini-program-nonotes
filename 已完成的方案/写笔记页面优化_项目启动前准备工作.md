# 写笔记页面优化 - 项目启动前准备工作

**准备日期**：2026-03-01  
**关联文档**：
- 写笔记页面优化_高优先级风险深入研究报告.md
- 写笔记页面优化_三阶段开发计划.md

---

## 一、审计结果总结

### 1.1 全代码路径审计

| 文件 | mentor/mood使用情况 | 状态 |
|-----|-------------------|------|
| write.js | ✅ 已识别，需修改 | ⏳ 待修改 |
| detail.js | ⚠️ mentor为null时fallback到'查理·芒格' | ⚠️ 需优化 |
| detail.wxml | ⚠️ 使用`letter.mentor || '查理·芒格'` | ⚠️ 需优化 |
| index.js | ✅ 列表页未直接使用mentor/mood展示 | ✅ 无问题 |
| replyToLetter/index.js | ✅ 已有fallback逻辑 | ✅ 无问题 |

---

### 1.2 详情页问题识别

**detail.js 第62-63行**：
```javascript
const mentorLocation = mentorLocations[result.data.mentor] || '奥马哈';
```

**detail.wxml 多处**：
```xml
<text class="mentor-name">致：{{letter.mentor || '查理·芒格'}}</text>
...
<text class="signature-text">—— {{letter.mentor || '查理·芒格'}}</text>
...
<text> {{letter.mentor || '查理·芒格'}}正在{{mentorLocation}}寓所研读...</text>
```

**问题**：当`mentor`为null时，fallback到'查理·芒格'，但对于"自己思考"的笔记，这不太准确。

**建议方案**：
- 当`mentor`为null时，不显示大师相关信息
- 添加`showMentor`标志控制显示

---

### 1.3 邮票默认值不一致问题

**发现问题**：当前代码中邮票默认值不一致！

| 位置 | 文件 | 默认值 | 状态 |
|------|------|--------|------|
| 1 | write.js data | 3张 | ⚠️ 需修改 |
| 2 | write.js fetchUserStamps() fallback | 3张 | ⚠️ 需修改 |
| 3 | replyToLetter/index.js 新用户初始化 | 2张 | ✅ 正确 |
| 4 | index.js fetchUserStamps() fallback | 3张 | ⚠️ 需修改 |

**需要统一修改的位置**：
1. write.js - 两处修改为2张
2. index.js - 一处修改为2张

---

## 二、启动前准备工作清单

### 2.1 准备工作1：代码审计与修复

| 任务 | 状态 | 预计时间 |
|-----|------|---------|
| 审计详情页mentor/mood处理 | ✅ 已完成 | - |
| 审计列表页mentor/mood处理 | ✅ 已完成 | - |
| 修改详情页mentor为null的展示 | ⏳ 待执行 | 0.5天 |
| 统一邮票默认值为2张（3处） | ⏳ 待执行 | 0.5天 |
| **小计** | | **1天** |

---

### 2.2 准备工作2：测试用例文档

| 任务 | 状态 | 预计时间 |
|-----|------|---------|
| Phase 1测试用例（14个） | ✅ 已定义 | - |
| Phase 2测试用例（18个） | ✅ 已定义 | - |
| Phase 3测试用例（4个） | ✅ 已定义 | - |
| 整理成测试用例文档 | ⏳ 待执行 | 1天 |
| **小计** | | **1天** |

---

### 2.3 准备工作3：回滚触发条件设计

| 任务 | 状态 | 预计时间 |
|-----|------|---------|
| 定义回滚触发条件 | ⏳ 待设计 | 0.5天 |
| 设计回滚决策流程 | ⏳ 待设计 | 0.5天 |
| 设计回滚通知机制 | ⏳ 待设计 | 0.5天 |
| **小计** | | **1.5天** |

---

### 2.4 准备工作4：开发计划优化

| 任务 | 状态 | 预计时间 |
|-----|------|---------|
| 开发周期11天→16天 | ✅ 已研究 | - |
| 更新开发计划文档 | ⏳ 待执行 | 0.5天 |
| **小计** | | **0.5天** |

---

### 2.5 准备工作总计

| 准备工作 | 预计时间 |
|---------|---------|
| 代码审计与修复 | 1天 |
| 测试用例文档 | 1天 |
| 回滚触发条件设计 | 1.5天 |
| 开发计划优化 | 0.5天 |
| **总计** | **4天** |

---

## 三、回滚触发条件设计

### 3.1 回滚触发条件

| 触发条件 | 严重程度 | 回滚范围 |
|---------|---------|---------|
| **严重Bug > 3个** | 🔴 高 | 相关Phase |
| **核心功能无法使用** | 🔴 高 | 全功能回滚 |
| **数据兼容性问题** | 🔴 高 | 相关Phase |
| **性能下降 > 30%** | 🟡 中 | 相关功能 |
| **（未来）用户反馈激增** | 🟡 中 | 相关功能 |

### 3.2 回滚决策流程

```
发现问题
    ↓
评估严重程度
    ↓
├─ 严重程度🔴高 → 立即回滚
│       ↓
│   创建回滚Pull Request
│       ↓
│   代码审查（1人审批）
│       ↓
│   执行回滚
│       ↓
│   通知团队
│       ↓
│   分析原因，制定修复方案
│
└─ 严重程度🟡中 → 评估是否回滚
        ↓
    讨论决定
        ↓
    回滚或继续修复
```

### 3.3 Git标签策略

| 标签 | 说明 | 创建时机 |
|------|------|---------|
| `v1.0.0-pre-optimize` | 优化前的稳定版本 | 项目启动前 |
| `v1.0.0-phase1` | Phase 1完成后的稳定版本 | Phase 1验收后 |
| `v1.0.0-phase2` | Phase 2完成后的稳定版本 | Phase 2验收后 |
| `v1.0.0-phase3` | Phase 3完成后的稳定版本 | Phase 3验收后 |

---

## 四、邮票默认值统一修改方案

### 4.1 需要修改的文件

#### 修改1：write.js

**位置1：data初始化**（第19行）
```javascript
data: {
  // ...
  userStamps: 2,  // 从3改为2
  // ...
}
```

**位置2：fetchUserStamps() fallback**（第106、110行）
```javascript
async fetchUserStamps() {
  // ...
  if (result.success && result.data.length > 0) {
    const userStamps = result.data[0].stamps;
    const stamps = userStamps !== undefined ? userStamps : 2;  // 从3改为2
    this.setData({ userStamps: stamps });
  } else {
    this.setData({ userStamps: 2 });  // 从3改为2
  }
}
```

#### 修改2：index.js

**位置：fetchUserStamps() fallback**（第64、68行）
```javascript
async fetchUserStamps() {
  // ...
  if (result.success && result.data.length > 0) {
    const userStamps = result.data[0].stamps;
    const stamps = userStamps !== undefined ? userStamps : 2;  // 从3改为2
    this.setData({ userStamps: stamps });
  } else {
    this.setData({ userStamps: 2 });  // 从3改为2
  }
}
```

---

## 五、详情页mentor为null优化方案

### 5.1 detail.js 修改

```javascript
// 在 loadLetterDetail() 中
const letter = {
  ...result.data,
  displayDate: cloudbaseUtil.formatDateTime(result.data.createTime),
  statusLabel: this.getStatusLabel(result.data.status),
  canShowReply: canShowReply,
  showMentor: !!result.data.mentor  // ← 新增：控制是否显示导师信息
};

const mentorLocation = result.data.mentor 
  ? mentorLocations[result.data.mentor] || '奥马哈' 
  : '';  // ← mentor为null时为空
```

### 5.2 detail.wxml 修改

```xml
<!-- 只在showMentor为true时显示导师相关信息 -->
<view class="letter-header" wx:if="{{letter.showMentor}}">
  <text class="mentor-name">致：{{letter.mentor}}</text>
</view>

<!-- 导师为空时，不显示"致：XXX" -->

<!-- 回复签名部分 -->
<view class="reply-signature" wx:if="{{letter.showMentor}}">
  <text class="signature-text">—— {{letter.mentor}}</text>
</view>

<!-- 等待提示部分 -->
<view wx:elif="{{letter.needReply && letter.showMentor}}" class="waiting-hint">
  <icon type="waiting" size="18" color="#8b4513"/>
  <text> {{letter.mentor}}正在{{mentorLocation}}寓所研读，回信约在18小时内送达</text>
</view>

<!-- 自己思考时，不显示等待提示 -->
```

---

## 六、测试用例文档结构

### 6.1 测试用例文档模板

```markdown
# 写笔记页面优化 - 测试用例文档

## Phase 1 测试用例

### T1.1 心境选择移除
| 用例ID | 测试场景 | 预期结果 | 优先级 | 状态 |
|-------|---------|---------|--------|------|
| T1.1.1 | 进入写笔记页面 | 不显示心境选择标签 | 🔴 高 | ⏳ 待测试 |
| ... |

### T1.2 规则说明弹窗移除
...

## Phase 2 测试用例
...

## Phase 3 测试用例
...
```

---

## 七、下一步行动

### 7.1 立即执行（今天）

- [ ] 创建优化前Git标签 `v1.0.0-pre-optimize`
- [ ] 修改write.js邮票默认值（2处）
- [ ] 修改index.js邮票默认值（1处）
- [ ] 修改detail.js和detail.wxml（mentor为null处理）

### 7.2 本周完成

- [ ] 整理测试用例文档
- [ ] 设计回滚触发条件文档
- [ ] 更新开发计划文档（11天→16天）

---

**准备工作文档结束**

**日期**：2026-03-01  
**版本**：v1.0
