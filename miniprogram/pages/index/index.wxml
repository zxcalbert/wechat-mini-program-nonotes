<side-menu
  show-menu="{{showMenu}}"
  user-stamps="{{userStamps}}"
  heatmap-data="{{heatmapData}}"
  bind:close="closeSideMenu"
/>

<view class="container">
  <!-- 页头：菜单按钮 + 标题 + 添加按钮 -->
  <view class="header">
    <text class="menu-toggle" bindtap="toggleSideMenu">☰</text>
    <text class="header-title">我的笔记</text>
    <view class="add-button" bindtap="goToWrite">
      <text class="add-icon">+</text>
    </view>
  </view>

  <!-- 笔记列表 -->
  <scroll-view scroll-y="true" class="notes-list" scroll-with-animation="{{true}}">
    <view wx:if="{{letters.length === 0}}" class="empty-state">
      <text class="empty-icon">📝</text>
      <text class="empty-text">还没有笔记呢</text>
      <text class="empty-subtext">点击右上角 + 开始写第一篇</text>
    </view>

    <view
      wx:for="{{letters}}"
      wx:key="_id"
      class="note-card"
      bindtap="goToDetail"
      bindlongpress="deleteLetter"
      data-id="{{item._id}}"
    >
      <view class="note-content-preview">{{item.content}}</view>

      <view class="note-footer">
        <text class="note-date">{{item.displayDate || '刚刚'}}</text>
      </view>
    </view>
  </scroll-view>

  <!-- 底部导航栏 -->
  <view class="bottom-nav">
    <view class="nav-item active" bindtap="navToHome">
      <text class="nav-icon">📋</text>
      <text class="nav-text">首页</text>
    </view>
    <view class="nav-item" bindtap="navToSearch">
      <text class="nav-icon">🔍</text>
      <text class="nav-text">搜索</text>
    </view>
    <view class="nav-item" bindtap="navToProfile">
      <text class="nav-icon">👤</text>
      <text class="nav-text">我的</text>
    </view>
  </view>
</view>
