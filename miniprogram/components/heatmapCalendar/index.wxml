<view class="heatmap-calendar">
  <view class="month-row" wx:for="{{weeks}}" wx:key="index">
    <view class="day-box {{day.intensity}}"
          wx:for="{{item}}"
          wx:for-item="day"
          wx:key="index">
    </view>
  </view>
</view>
