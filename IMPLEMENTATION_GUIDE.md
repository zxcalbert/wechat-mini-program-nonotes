# 投资笔记应用 - 完整实现说明

## 目录
1. [功能概述](#功能概述)
2. [UI/UX 改进](#uiux-改进)
3. [核心功能](#核心功能)
4. [数据模型](#数据模型)
5. [API 和云函数](#api-和云函数)
6. [安装和配置](#安装和配置)

---

## 功能概述

本次更新实现了以下主要功能：

### 1. 界面重构
- ✅ 移除了"我的信箱"和"给导师写信"按钮
- ✅ 登录后默认进入笔记列表页面
- ✅ 移除了下方的 TabBar 导航
- ✅ 添加左上角汉堡菜单（三条横线）

### 2. 导航菜单
- ✅ 左上角菜单展示写作热力图（日期分布点阵图）
- ✅ 菜单选项：
  - 回收站（删除的笔记可恢复）
  - 字体设置（占位符）
  - 邮票管理（查看和购买邮票）
  - 关于应用
  - 退出登录

### 3. 浮动按钮
- ✅ 主页下方中间的圆形"+"按钮
- ✅ 点击后直接跳转到写笔记页面

### 4. 邮票系统
- ✅ 新用户获得 3 张免费邮票
- ✅ 写笔记时可选择是否需要大师回信
- ✅ 选择"需要回信"会消耗 1 张邮票
- ✅ 每张邮票用完后需要购买

### 5. 回复延迟机制
- ✅ 请求回复后需等待 18 小时
- ✅ 这个延迟帮助投资者深入思考
- ✅ 每天最多只能寄信一次

---

## UI/UX 改进

### 首页（index）
```
┌────────────────────────────────┐
│ ☰  我的笔记                    │ ← 汉堡菜单 + 标题
├────────────────────────────────┤
│                                │
│  [笔记卡片 1]                   │
│  心境：平和 | 致：查理·芒格      │
│  内容预览...                    │
│  2024-01-25                     │
│                                │
│  [笔记卡片 2]                   │
│  ...                            │
│                                │
│              ┌───┐              │
│              │ + │              │ ← 浮动写笔记按钮
│              └───┘              │
└────────────────────────────────┘
```

### 侧边菜单（sideMenu）
```
┌─────────────────────┐
│ 更多  ✕             │
├─────────────────────┤
│ 写作热力分布        │
│ [热力日历图表]      │
├─────────────────────┤
│ 🗑️  回收站          │
│ 🔤  字体设置        │
│ 🎫  邮票(3/3)       │
│ ℹ️  关于应用        │
│ ↪️  退出登录        │ (红色)
└─────────────────────┘
```

### 写笔记页面（write）
```
┌────────────────────────────────┐
│ ← 写笔记                      │
├────────────────────────────────┤
│                                │
│ 指引导师：[查理·芒格▼]          │
│ 当前心境：[平和] [焦虑] ...    │
│                                │
│ ┌──────────────────────────┐  │
│ │ 致：查理·芒格  2024-01-25│  │
│ ├──────────────────────────┤  │
│ │                          │  │
│ │  笔记内容...             │  │
│ │  (输入区域)              │  │
│ │                          │  │
│ └──────────────────────────┘  │
│ 当前字数：0                    │
│                                │
│ 📮 需要大师回信吗？             │
│ [无需回复] [需要回信]          │
│                                │
│ (如果需要回信，显示说明和邮票数)│
│                                │
│ [📝 寄出笔记] (绿色按钮)      │
│                                │
└────────────────────────────────┘
```

---

## 核心功能

### 1. 每日限制
- 每个用户每天只能寄信一次
- 实现位置：`write.js` 的 `checkDailyLimit()` 方法
- 检查：`lastWriteDate` 是否是今天

```javascript
async checkDailyLimit() {
  // 检查今天是否已写过
  const today = new Date();
  const startOfDay = new Date(today.getFullYear(), today.getMonth(), today.getDate());
  const result = await cloudbaseUtil.query('letters', {
    where: {
      _openid: this.data.openid,
      createTime: { $gte: startOfDay.getTime() }
    }
  });
  
  if (result.success && result.data.length > 0) {
    // 今天已写过，不允许再写
  }
}
```

### 2. 邮票消耗
- 在 `submitLetter()` 方法中实现
- 用户选择"需要回信"时消耗邮票
- 自动从 `users` 集合中扣除

```javascript
if (needReply) {
  // 扣除邮票
  const newStamps = Math.max(0, userStamps - 1);
  await cloudbaseUtil.update('users', userDoc._id, {
    stamps: newStamps
  });
  
  // 记录历史
  await db.collection('stampHistory').add({
    data: {
      action: '使用1张邮票请求回信',
      change: -1
    }
  });
}
```

### 3. 18小时延迟回复
- 在 `submitLetter()` 中设置 `replyExpectTime`
- 计算方式：当前时间 + 18小时

```javascript
replyExpectTime: needReply ? 
  new Date(Date.now() + 18 * 60 * 60 * 1000).getTime() : null
```

### 4. 热力图生成
- `heatmapCalendar` 组件：按周显示日期点阵图
- 每个方块代表一天，颜色深度代表该天的笔记数
- 颜色等级：
  - 灰色：0篇笔记
  - 浅绿：1篇笔记
  - 中绿：2篇笔记
  - 深绿：3+篇笔记

---

## 数据模型

### letters 集合
```
{
  _id: ObjectId,
  _openid: string,
  mentor: string,           // 查理·芒格 | 巴菲特 | 段永平
  mood: string,             // 焦虑 | 贪婪 | 平和 | 困惑
  content: string,          // 笔记内容（最长5000字）
  status: string,           // pending | replied | saved
  needReply: boolean,       // 是否请求回信
  reply: string,            // 大师回复（可选）
  createTime: timestamp,
  replyTime: timestamp,     // 实际回复时间
  replyExpectTime: timestamp, // 预计回复时间
  wordCount: number,
  deleted: boolean          // 软删除标记
}
```

### users 集合
```
{
  _id: ObjectId,
  _openid: string,
  stamps: number,           // 当前邮票数
  totalPurchased: number,   // 总购买邮票数
  lastWriteDate: date,      // 最后写信日期（用于每日限制）
  createdAt: timestamp
}
```

### stampHistory 集合
```
{
  _id: ObjectId,
  _openid: string,
  action: string,           // 操作描述
  change: number,           // 邮票变化数
  price: number,            // 购买价格（可选）
  time: timestamp
}
```

---

## API 和云函数

### 现有云函数需要更新

#### replyToLetter 云函数
需要支持以下功能：
1. 检查邮票是否足够
2. 调用 DeepSeek API 生成回复
3. 更新 letters 集合中的 reply 字段
4. 设置 replyTime 为当前时间

```javascript
// 伪代码
exports.main = async (event, context) => {
  const { letterId, mentor, mood, content } = event;
  
  // 1. 验证邮票
  const user = await getUser(context.openid);
  if (user.stamps <= 0) {
    return { success: false, error: '邮票不足' };
  }
  
  // 2. 调用 DeepSeek API
  const reply = await deepseekAPI.generate({
    mentor,
    mood,
    content
  });
  
  // 3. 保存回复
  await updateLetter(letterId, {
    reply: reply,
    status: 'replied',
    replyTime: new Date()
  });
  
  // 4. 记录邮票消耗
  await recordStampUsage(context.openid, -1);
  
  return { success: true, reply };
};
```

### 新增云函数需求

#### generateReply 云函数（可选）
如果要延迟生成回复，可以创建定时云函数：
- 每小时检查一次待回复的笔记
- 如果已过 18 小时，立即生成回复

---

## 安装和配置

### 1. 数据库初始化

在 CloudBase 控制面板创建以下集合：

```sql
-- letters 集合
CREATE COLLECTION letters (
  _id,
  _openid,
  mentor,
  mood,
  content,
  status,
  needReply,
  reply,
  createTime,
  replyTime,
  replyExpectTime,
  wordCount,
  deleted
);

-- users 集合
CREATE COLLECTION users (
  _id,
  _openid,
  stamps,
  totalPurchased,
  lastWriteDate,
  createdAt
);

-- stampHistory 集合
CREATE COLLECTION stampHistory (
  _id,
  _openid,
  action,
  change,
  price,
  time
);
```

### 2. 安全规则配置

参考 `DATABASE_SCHEMA.md` 中的安全规则部分。

### 3. 云函数部署

需要部署或更新以下云函数：
- `replyToLetter`: 处理大师回复逻辑
- `deepseekAPI`: (可选) 集成 DeepSeek API

### 4. 前端配置

所有前端代码已完成，直接在微信开发者工具中：
1. 编译项目
2. 预览或上传

---

## 文件结构

```
miniprogram/
├── pages/
│   ├── index/              # 首页（笔记列表）
│   │   ├── index.wxml      ✅ 已更新
│   │   ├── index.js        ✅ 已更新
│   │   ├── index.json      ✅ 已更新
│   │   └── index.wxss      ✅ 已更新
│   ├── write/              # 写笔记页面
│   │   ├── write.wxml      ✅ 已更新
│   │   ├── write.js        ✅ 已更新
│   │   ├── write.json      ✅ 已更新
│   │   └── write.wxss      ✅ 已更新
│   ├── stamps/             # 邮票管理页面
│   │   ├── stamps.wxml     ✅ 新建
│   │   ├── stamps.js       ✅ 新建
│   │   ├── stamps.json     ✅ 新建
│   │   └── stamps.wxss     ✅ 新建
│   ├── login/              # 登录页面
│   ├── detail/             # 笔记详情页面
│   └── trash/              # 回收站页面 (TODO)
│
├── components/
│   ├── sideMenu/           # 侧边菜单组件
│   │   ├── index.wxml      ✅ 新建
│   │   ├── index.js        ✅ 新建
│   │   ├── index.json      ✅ 新建
│   │   └── index.wxss      ✅ 新建
│   ├── heatmapCalendar/    # 热力日历组件
│   │   ├── index.wxml      ✅ 新建
│   │   ├── index.js        ✅ 新建
│   │   ├── index.json      ✅ 新建
│   │   └── index.wxss      ✅ 新建
│   └── cloudTipModal/
│
├── utils/
│   └── cloudbaseUtil.js    # 数据库工具（已有 update 方法）
│
├── app.js
├── app.json                ✅ 已更新（移除 tabBar）
├── app.wxss
└── sitemap.json
```

---

## 待做事项

### 高优先级
1. **云函数更新** - 更新 `replyToLetter` 支持 18 小时延迟
2. **数据库初始化** - 创建 users 和 stampHistory 集合
3. **安全规则** - 配置数据库访问权限
4. **测试** - 完整的功能测试

### 中优先级
1. **回收站页面** - 实现被删除笔记的查看和恢复
2. **关于页面** - 显示应用信息
3. **支付集成** - 真实的微信支付接口
4. **邮票使用记录** - 详细的历史记录统计

### 低优先级
1. **字体设置** - UI 完成，功能待实现
2. **数据导出** - 导出笔记为 PDF/Word
3. **分享功能** - 分享笔记到微信
4. **搜索功能** - 笔记全文搜索

---

## 测试场景

### 场景 1：首次使用
1. ✅ 用户登录
2. ✅ 显示空笔记列表 + 提示信息
3. ✅ 点击"+"按钮进入写笔记
4. ✅ 写入 100+ 字的笔记
5. ✅ 选择是否需要回信
6. ✅ 如果选择"需要回信"，消耗 1 张邮票（从 3 变为 2）

### 场景 2：每日限制
1. ✅ 用户今天已写过笔记
2. ✅ 再次打开写笔记页面，显示"今天已经寄信过了"提示
3. ✅ 3 秒后自动返回首页

### 场景 3：邮票用完
1. ✅ 用户邮票数为 0
2. ✅ 选择"需要回信"时，显示购买邮票提示
3. ✅ 点击"购买邮票"跳转到邮票页面

### 场景 4：侧边菜单
1. ✅ 点击左上角"☰"打开侧边菜单
2. ✅ 显示热力图、菜单项和用户操作
3. ✅ 点击菜单项进行相应操作
4. ✅ 点击背景或"✕"关闭菜单

---

## 常见问题

### Q: 邮票是否支持真实支付？
A: 当前实现是模拟支付流程。需要集成微信支付 API 来实现真实支付。

### Q: 18 小时延迟如何实现？
A: 通过在 letters 集合中存储 `replyExpectTime` 字段。前端显示"等待中"状态，云函数定时检查并生成回复。

### Q: 每天只能写一次，如何检查？
A: 在 write 页面的 `checkDailyLimit()` 中查询当天是否有已发送的笔记。

### Q: 热力图如何获取数据？
A: 从 letters 集合按日期分组统计，计算每天的笔记数。

---

## 联系方式

如有问题或建议，请提交 Issue 或联系开发者。
